planning_period: 0.01 # seconds

max_unseen_duration: 10.0 # seconds

glancing:
  yaw_rate: 1.57 # radians per second
  duration: 1.0 # seconds

approach_speed: 6.0 # metres per second
chase_speed: 6.0 # metres per second
catch_trigger_distance: 2.5 # metres

yawing:
  max_ball_distance: 666.0 # metres

lurking:
  z_offset: -1.5 # metres
  observe_dist: 5 # metres
  max_dist_from_trajectory: 5 # metres
  max_reposition: 1.0 # metres
  min_observing_duration: 3.0 # the target must be observed at least this amount of seconds to start lurking
  min_last_points: 10 # how many last points at minimum to use when RANSACing the lurking position
  min_last_duration: 3 # how many last sedonds at minimum to use when RANSACing the lurking position

trajectory:
  sampling_dt: 0.2 # seconds
  horizon: 8.0 # seconds
  target_offset: 5.0 # metres

z_bounds:
  min: 5.0
  max: 20.0

# constraint_ranges:
#   {
#     "ballcatch_fast":   3.00,
#     "ballcatch_medium": 8.00,
#     "ballcatch_slow":   .inf,
#   }

constraint_states:
  {
    "lost_glancing": "ballcatch_slow",
    "waiting_for_detection": "ballcatch_slow",
    "yawing_detection": "ballcatch_slow",
    "following_detection": "ballcatch_slow",
    "following_prediction": "ballcatch_fast",
    "going_to_lurk": "ballcatch_fast",
    "lurking": "ballcatch_medium",
  }

start_position:
  [
    -30.0,   # x coordinate (metres)
    0.0,  # y coordinate (metres)
    8.5,   # z coordinate (metres)
    -0.1    # yaw (radians)
  ]
