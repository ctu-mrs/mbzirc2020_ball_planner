\documentclass{standalone}
\usepackage{pgfplots}
\usepackage{bm}
\usepackage{siunitx}
\usepackage[export]{adjustbox}
\usetikzlibrary{arrows, decorations.pathmorphing, backgrounds, positioning,fit,petri}
\usetikzlibrary{calc}

\usepackage{filecontents}

\def\axisdefaultwidth{8cm}
\def\axisdefaultheight{3cm}
\def\cGreen{green!70!black}
% \pgfplotsset{every axis/.append style={
%                     legend style={font=\tiny,line width=.5pt,mark size=60pt},
%                     }}
\begin{document}
\pgfplotsset{
    % use this `compat' level or higher to use the LUA backend for calculation
    % (--> speed improvement)
    compat=1.15,
  }

\begin{tikzpicture}
  \pgfplotsset{
      scale only axis,
      % scaled x ticks=false,
      grid=both,
      grid style={line width=.1pt, draw=black, dotted},
      major grid style={line width=.2pt,draw=black, dotted},
      legend pos=south east,
      % yticklabel style={
      %         /pgf/number format/fixed,
      %         /pgf/number format/precision=5
      % },
      % scaled y ticks=false
      % legend style={font=\small},
  }
  \begin{axis}
    [
    ytick={0,0.2,...,1},
    xtick={0,0.2,...,2},
    xmin=0,
    xmax=2,
    xlabel={localization error (\si{\metre})},
    ylabel={cumulative frequency},
    y label style={at={(axis description cs:-0.08,.5)},anchor=south},
    ymin = 0,
    ymax = 1,
    legend style={at={(1,0)}, anchor=south east},
    % const plot mark mid
    % bar width=5.7,
    ]
    \addplot [red, mark=o, mark size=0.9, thick, miter limit=1]
    % \addplot [red, fill, fill opacity=0.33]
       table [x=err, y=acclassA, col sep=comma] {../output/class_out.csv};
    \addplot [\cGreen, mark=x, mark size=1.5, thick, miter limit=1]
    % \addplot [\cGreen, fill, fill opacity=0.33]
       table [x=err, y=acclassB, col sep=comma] {../output/class_out.csv};
    \addplot [blue, mark=square, mark size=0.9, thick, miter limit=1]
    % \addplot [blue, fill, fill opacity=0.33]
       table [x=err, y=acclassC, col sep=comma] {../output/class_out.csv};
    \legend{case \textbf{A}, case \textbf{B}, case \textbf{C}}
  \end{axis}

  % ensure same size of both graphs
  \node [minimum width=9.3cm, minimum height=3.4cm, anchor=south west] {};

\end{tikzpicture}
% dist, err, recall, pdet, pcclassA, pcclassB, pcclassC

\end{document} 




