\documentclass{standalone}
\usepackage{pgfplots}
\usepackage{bm}
\usepackage{siunitx}
\usepackage[export]{adjustbox}
\usetikzlibrary{arrows, decorations.pathmorphing, backgrounds, positioning,fit,petri}
\usetikzlibrary{calc}

\usepackage{filecontents}

\def\axisdefaultwidth{8cm}
\def\axisdefaultheight{3cm}
\def\cGreen{green!70!black}
% \pgfplotsset{every axis/.append style={
%                     legend style={font=\tiny,line width=.5pt,mark size=60pt},
%                     }}
\begin{document}

\begin{tikzpicture}
  \pgfplotsset{
      scale only axis,
      % scaled x ticks=false,
      grid=both,
      grid style={line width=.1pt, draw=black, dotted},
      major grid style={line width=.2pt,draw=black, dotted},
      legend pos=south east,
      % legend style={font=\small},
  }
  \begin{axis}
    [
    ytick={0,25,...,100},
    xtick={0,10,...,90},
    xmin=0,
    xmax=90,
    xlabel={sensor-target distance (\si{\metre})},
    ylabel={class recall (\%)},
    y label style={at={(axis description cs:0.08,.5)},anchor=south},
    ymin = 0,
    ymax = 100,
    legend style={at={(1,1)}, anchor=north east}
    ]
    \addplot [red, mark=*, mark size=0.9, thick, miter limit=1]
       table [x=dist, y expr={100.0*\thisrow{pcclassA}}, col sep=comma] {../output/stats_out.csv};
    \addplot [\cGreen, mark=*, mark size=0.9, thick, miter limit=1]
       table [x=dist, y expr={100.0*\thisrow{pcclassB}}, col sep=comma] {../output/stats_out.csv};
    \addplot [blue, mark=*, mark size=0.9, thick, miter limit=1]
       table [x=dist, y expr={100.0*\thisrow{pcclassC}}, col sep=comma] {../output/stats_out.csv};
    \legend{case \textbf{A}, case \textbf{B}, case \textbf{C}}
  \end{axis}

  % ensure same size of both graphs
  \node [minimum width=9.3cm, minimum height=3.4cm, anchor=south west] {};

\end{tikzpicture}
% dist, err, recall, pdet, pcclassA, pcclassB, pcclassC

\end{document} 



